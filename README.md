To start work with this code, you should have configured AWS configure with your credentials. 
After this, you should run these commands:
terraform init
terraform apply. 
These commands create an EKS Cluster with one node group. 
